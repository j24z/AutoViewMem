You are designing extraction views for a long-term user memory system.

Read the following conversation snippet. Identify what types of structured, persistent information should be extracted so that the system can answer future questions about the user across many conversations.

Generate 5–10 extraction instructions. Each instruction must:
- Target a distinct memory view (no overlap)
- Focus on user-centric, reusable information (not turn-specific summaries)
- Be concrete and directly actionable for structured extraction
- Prefer facts, attributes, relationships, preferences, intents, or patterns over abstract analysis

Conversation Snippet:
{text_chunk}

Output Format:
A JSON list of strings only.
Each string should be an imperative extraction instruction, e.g.:
"Extract explicit user preferences about food, travel, or hobbies"
"Extract named people and the user's relationship to them"

Return ONLY the JSON list. Do not include explanations.
